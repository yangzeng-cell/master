```
页面级别优化
	1.减少HTTP请求次数
		　(1). 从设计实现层面简化页面
		　(2). 合理的设置http请求
		　(3)  资源合并与压缩
		　		对js，css,img文件进行压缩，小图片可以使用base64格式的
		　(4). CSS Sprites
	2.将外部脚本置于底部、或者使用script属性defer
	3.将CSS放到head里面
	4.减少不必要的http跳转
代码级优化
	1.对集合的属性length进行缓存，避免重复调用，把需要重复调用的属性值进行缓存
	2.减少回流和重绘
	3.css减少过深的层级嵌套
	4.不要使用with,eval
	5.不要使用构造函数创建函数
	6.对频繁触发的事件需要节流和防抖
	7.使用事件委托来减少事件的绑定
```

